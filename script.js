const header=document.querySelector('#header');const menu=document.querySelector('.menu-button');const mobile=document.querySelector('.mobile-nav');window.addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>30));menu.addEventListener('click',()=>{const open=mobile.classList.toggle('open');menu.setAttribute('aria-expanded',open);menu.textContent=open?'CLOSE':'MENU'});mobile.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobile.classList.remove('open');menu.textContent='MENU';menu.setAttribute('aria-expanded','false')}));document.querySelector('#signup').addEventListener('submit',e=>{e.preventDefault();document.querySelector('#form-note').textContent='V1 PROTOTYPE — SIGNUP BACKEND COMING NEXT.';e.target.reset()});

const sectionAnchors={watch:'.section-head',mission:'.portrait',shop:'.section-head',ignited:'.ignited-content',book:'.epk-card',support:'.kicker',join:'.kicker'};const sectionCorrections={ignited:-56,support:-64};document.querySelectorAll('a[href^="#"]').forEach(link=>{link.addEventListener('click',e=>{const id=link.getAttribute('href').slice(1);const target=document.getElementById(id);if(!target)return;e.preventDefault();const selector=sectionAnchors[id];const anchor=selector?target.querySelector(selector):target;const headerHeight=64;let top;if((id==='mission'||id==='book')&&anchor){const rect=anchor.getBoundingClientRect();const visibleCenter=headerHeight+(window.innerHeight-headerHeight)/2;top=rect.top+window.scrollY+(rect.height/2)-visibleCenter}else{const breathingRoom=24;const correction=sectionCorrections[id]??0;top=(anchor||target).getBoundingClientRect().top+window.scrollY-headerHeight-breathingRoom+correction}window.scrollTo({top,behavior:'smooth'});history.replaceState(null,'','#'+id)})});